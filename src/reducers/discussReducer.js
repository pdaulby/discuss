export default function discussReducer(state = [], action) {
    switch(action.type) {
        case 'CREATE_POINT':
        //TODO
            return state;

        default: 
            return state;
    }
}